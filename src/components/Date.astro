---
const { weekday, date } = Astro.props;
---

<div class="flex flex-col items-center justify-center">
    <span class="text-sm">{weekday.toUpperCase()}</span>
    <span class="text-3xl">{date}</span>
</div>